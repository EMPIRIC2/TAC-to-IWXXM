# Stub Secret — replace values out-of-band before apply (never commit real secrets).
# Required keys: DATABASE_URL, SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY
# Optional: SUPABASE_JWKS_URL, DISSEMINATION_EGRESS_ALLOWLIST, SUPABASE_SECRET_KEY
apiVersion: v1
kind: Secret
metadata:
  name: metar-api-secrets
  labels:
    app.kubernetes.io/name: metar-api
    app.kubernetes.io/component: api
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME_DO_POSTGRES_URL"
  SUPABASE_URL: "REPLACE_ME_SUPABASE_AUTH_URL"
  SUPABASE_JWKS_URL: ""
  SUPABASE_PUBLISHABLE_KEY: "REPLACE_ME_SUPABASE_PUBLISHABLE_KEY"
  SUPABASE_SECRET_KEY: "REPLACE_ME_OPTIONAL"
  DISSEMINATION_EGRESS_ALLOWLIST: ""
