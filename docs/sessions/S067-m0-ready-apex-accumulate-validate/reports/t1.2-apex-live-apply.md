# Live apply — #948 apex redirect (2026-08-16)

> **Corpus**: [Corpus: deploy] [Corpus: product §F30]  
> **Decisions**: `D-S067-948-apply=1a`, `D-S067-948-redirect=1a`

## What ran

1. Confirmed Dig `@1.1.1.1` / `@8.8.8.8` / local: `tac-to-iwxxm.com` + `www` → `168.144.12.70`.
2. `permanent-redirect` + `$request_uri` **denied** by ingress-nginx v1.12.1 webhook.
3. Applied **only** `redirect-apex.yaml` + `ingress-frontend-apex.yaml` on
   `do-nyc1-metar-iwxxm` / `metar-iwxxm` (not the full prod overlay).
4. HTTP-01 challenges **valid**; `metar-frontend-apex-tls` **Ready**.

## UJ-OPS-002 / TC-EV057-948

| Check | Result |
|-------|--------|
| HTTPS apex path/query | 301 → `https://app.tac-to-iwxxm.com/foo?bar=1` |
| HTTPS www | 301 → `https://app.tac-to-iwxxm.com/` |
| HTTP apex | 301 → HTTPS app with path/query |
| TLS SAN | `tac-to-iwxxm.com`, `www.tac-to-iwxxm.com` |
| App host | still HTTP/2 200 |

Staging short-host manifests updated the same way; **not applied** (needs A → `143.244.202.13`).
