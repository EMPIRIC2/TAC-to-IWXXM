# T3.1 spike — POST /api/v1/validate for validate-only IWXXM (#838)

> Session: S067 / EV-057 · 2026-08-16  
> Corpus: [Corpus: api] [Corpus: product §F7.s] [Corpus: product §F2]

## Question

Does existing `POST /api/v1/validate` accept paste XML / single-file content **without** TAC convert?

## Findings

1. **Form path**: `xml_content` Form field; when non-empty, convert is skipped
   (`apps/backend/src/api.py` — `if not xml_content: convert…`).
2. **JSON path**: `ValidateRequest.iwxxm_xml` sets `xml_content` and clears `manual_text`.
3. **FE client**: `validateIwxxm({ xmlContent })` already appends `xml_content` only
   (`apps/frontend/src/utils/api.ts`).
4. **Unit coverage**: `test_api_routes_validation_unit.py` posts `xml_content` alone;
   msgspec/TC-F11 parity fixtures use XML-only multipart.

## Gap?

**None for wire.** Dedicated Validate UI can call `validateIwxxm` with paste or file text.
Multi-file / zip remains deferred per F7.s AC.

## T3.5

Doc note only: reuse existing validate endpoint — no api-contract delta.
