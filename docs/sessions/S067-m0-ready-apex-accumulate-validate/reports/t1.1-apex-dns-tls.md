# T1.1 — Apex DNS/TLS probe (EV-057 / #948)

> **Date**: 2026-08-15 · **Updated**: 2026-08-16 (live cutover) · **Task**: T1.1  
> **Corpus**: [Corpus: deploy] [Corpus: product §F30]

## Findings (2026-08-15 — pre-cutover)

| Host | DNS | HTTPS then | Cert SAN |
|------|-----|-------------|----------|
| `tac-to-iwxxm.com` | A `207.207.210.50`, `207.207.210.36` (Porkbun) | **302** → `https://tac-to-iwxxm-com.l.ink/` | LE: `tac-to-iwxxm.com`, `*.tac-to-iwxxm.com` |
| `www.tac-to-iwxxm.com` | CNAME `uixie.porkbun.com` → same A | **302** → same `l.ink` | same LE cert |
| `app.tac-to-iwxxm.com` | A **`168.144.12.70`** (prod DOKS LB) | **200** app | LE: `app.tac-to-iwxxm.com` only |

HTTP apex/www also 302 → `l.ink` (openresty / Porkbun parking).

## Live cutover (2026-08-16)

Public + local Dig: apex/`www` **A `168.144.12.70`**. Prod apply (`D-S067-948-apply=1a`,
`D-S067-948-redirect=1a`): `metar-apex-redirect` + `metar-frontend-apex`.
cert-manager issued `metar-frontend-apex-tls` (SAN apex + www). UJ-OPS-002 curl:

| Request | Result |
|---------|--------|
| `https://tac-to-iwxxm.com/foo?bar=1` | **301** `Location: https://app.tac-to-iwxxm.com/foo?bar=1` |
| `https://www.tac-to-iwxxm.com/` | **301** `Location: https://app.tac-to-iwxxm.com/` |
| `http://tac-to-iwxxm.com/foo?bar=1` | **301** → HTTPS app URL (path/query preserved) |
| `https://app.tac-to-iwxxm.com/` | **200** (no loop) |

ingress-nginx v1.12 webhook rejected `$request_uri` on `permanent-redirect`; snippets off.
Redirect is the nginx pod, not an Ingress annotation.
