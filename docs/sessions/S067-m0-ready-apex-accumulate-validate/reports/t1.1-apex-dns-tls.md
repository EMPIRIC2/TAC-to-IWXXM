# T1.1 — Apex DNS/TLS probe (EV-057 / #948)

> **Date**: 2026-08-15 · **Task**: T1.1 · **Corpus**: [Corpus: deploy] [Corpus: product §F30]

## Findings

| Host | DNS | HTTPS today | Cert SAN |
|------|-----|-------------|----------|
| `tac-to-iwxxm.com` | A `207.207.210.50`, `207.207.210.36` (Porkbun) | **302** → `https://tac-to-iwxxm-com.l.ink/` | LE: `tac-to-iwxxm.com`, `*.tac-to-iwxxm.com` |
| `www.tac-to-iwxxm.com` | CNAME `uixie.porkbun.com` → same A | **302** → same `l.ink` | same LE cert |
| `app.tac-to-iwxxm.com` | A **`168.144.12.70`** (prod DOKS LB) | **200** app | LE: `app.tac-to-iwxxm.com` only |

HTTP apex/www also 302 → `l.ink` (openresty / Porkbun parking).

## Gap (blocks live redirect)

Apex and `www` do **not** point at the prod LB. Sibling Ingress on DOKS cannot terminate apex traffic until DNS (and preferably HTTP-01 challenge path) targets `168.144.12.70`.

## Cutover checklist (ops — before #948 Done / live AC)

1. At Porkbun (or DNS host): set apex **A** → `168.144.12.70` (remove parking A/ALIAS).
2. Set `www` **A** or **CNAME** to LB / apex as preferred (prefer A → LB for cert-manager HTTP-01).
3. Merge/apply sibling Ingress `metar-frontend-apex` (T1.2) so cert-manager can issue for apex (+ www).
4. Verify: `curl -sI https://tac-to-iwxxm.com/foo?bar=1` → **301/308** `Location: https://app.tac-to-iwxxm.com/foo?bar=1`.
5. Retire Porkbun URL forward / `l.ink` parking page.

## Decision for M1 repo work

Per `D-S067-07-kube=1a`: land Ingress manifests on the evolve branch → `stage`; live DNS cutover + prod apply at promote (or earlier if user chooses). T1.1 status: **gap documented** — not a code blocker for manifests.
