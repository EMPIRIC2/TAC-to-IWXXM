# T2.3 — TC SIGMET STNR / exceptional geometry (TC-EV030-004 / #829)

> **Decision**: `D-S037-T2.3-oos` — STNR **in-cycle**; exceptional geometry beyond A6-2-TC **OOS with cite** (S02.M2).  
> **Date**: 2026-08-03 · EV-030 / S037

## In scope (covered)

| Shape | Coverage | Codes / fixtures |
|-------|----------|------------------|
| TC + `STNR` (accept) | Pack theme TC | `STNR_MOVEMENT` · `accept/sigmet_tc_stnr.tac` |
| TC + `STNR` + `MOV` (negative) | Pack theme TC | `INVALID_STNR_MOVEMENT` · `negative/sigmet/tc_stnr_with_mov.tac` |
| `WI … OF TC CENTRE` | Pack theme TC | `TC_CB_GEOMETRY` + shared `POLYGON_LOCATION` · A6-2-TC |

Shared G1 STNR rules already apply to TC bodies; T2.1/T2.2 pinned them on TC TAC.

## Out of scope this cycle (explicit cite)

| Residual | Why OOS | Cite |
|----------|---------|------|
| Relative FIR / half-plane phrases (`N OF` / `E OF` FIR, etc.) on TC SIGMET | Shared SIGMET geometry backlog — encode-engine work, not TC-lint-only | [#797](https://github.com/EMPIRIC2/TAC-to-IWXXM/issues/797) (closed backlog); reopen **child of #829** only if encode returns |
| Arbitrary polygon / line CRS heuristics beyond A6-2-TC `WI … OF TC CENTRE` | Same G1 residual as general SIGMET; no TC-specific encode milestone this cycle | S025 G1 residual; `COVERAGE_MATRIX` G1 row; S02.M2 |
| New encode paths for exceptional TC geometry | Out of EV-030 encode budget; M7 quality path already green for A6-2-TC | #829 AC2; E30-T4 unlock is catalog-only (T2.4) |

## #829 AC2 mapping

- [x] STNR on TC bodies covered (accept + STNR+MOV negative)
- [x] Exceptional geometry beyond happy path **explicitly OOS with cite** (this note + pack test)

## Next

**T2.4** — unlock `sigmet-A6-2-TC` catalog tier (`wmoPass` / `wmoReference` per ADR-032) while M7 quality path stays green.
