# T7.1 — Playwright + live on provisional DOKS URLs (S038 / EV-031)

> **Date**: 2026-08-03  
> **Branch**: `evolve/EV-031-platform-independence-842`  
> **Target**: LB `168.144.12.70` + Host-header placeholders (`D-S038-t63-waive`)

## Result

**PASS — 13/13** via `make test-live-e2e-doks-provisional` (~14s).

| Suite | Cases | Status |
|-------|------:|--------|
| `uj045-047-f31-hybrid-sessions.e2e.spec.ts` | TC-F31-001..006 / UJ-045..047 | PASS |
| `auth.e2e.spec.ts` | optional Auth + public convert | PASS |
| `public-app-f21-f22.e2e.spec.ts` | UJ-001 + F22 + Auth restored | PASS |

## Harness

- `scripts/deploy/doks_provisional_live_env.sh` — forces provisional `LIVE_*` (overrides Render defaults / local `PLAYWRIGHT_BASE_URL`)
- Chromium `--host-resolver-rules` maps placeholder FE/API hosts → LB IP (no `/etc/hosts`)
- Playwright `request` fixture hits LB IP + `Host: api.doks…` via `playwrightApiFetch`

## Blockers found & unblocked

1. **work-sessions 500** — sync SQLAlchemy kept `?ssl=require` from asyncpg `DATABASE_URL`; psycopg requires `sslmode=require`.  
   - Code fix: `apps/backend/src/services/work_session_service.py` `_sync_database_url`  
   - Live interim: ConfigMap mount + `scripts/ops/apply_doks_work_session_ssl_fix.sh` (GHCR `write:packages` unavailable for image republish)
2. **FE `main-latest` lacked F31 UI** (`guest-loss-notice` / Sign in).  
   - Built `metar-frontend:ev031-doks` locally; hot-copied `dist` into running FE pod (node SSH / GHCR push blocked)

## Spec coverage

- TC-F31-001/002 — public convert + guest loss notice  
- TC-F31-003 — JWT gates work-sessions; convert public  
- TC-F31-004 — login + auto-upload toast  
- TC-F31-005 — privacy discloses Auth cookies + work-history toggle  
- TC-F31-006 — Sign in entry from guest notice  

## Residual

- Republish `backend:ev031-doks` + `frontend:ev031-doks` to `ghcr.io/empiric2/...` when a token with `write:packages` is available; remove ConfigMap mount + FE hot-copy.
- T7.2 H4–H5 connectivity next; T6.5 still soak-calendar blocked.
