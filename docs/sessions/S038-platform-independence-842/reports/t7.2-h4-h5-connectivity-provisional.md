# T7.2 — H4–H5 connectivity on provisional DOKS URLs (S038 / EV-031)

> **Date**: 2026-08-03  
> **Branch**: `evolve/EV-031-platform-independence-842`  
> **Target**: LB `168.144.12.70` + Host-header placeholders (`D-S038-t63-waive`)  
> **Spec**: connectivity-gates H4–H5; `D-S038-tp`; test-plan H4–H5 required

## Result

**PASS** via `make test-live-connectivity-doks-provisional` (~1s after API awake).

| Tier | Checks | Status |
|------|-------:|--------|
| H0c | CORS policy unit (`tests/unit/test_cors_policy.py`) | 6/6 PASS |
| H4 | Live CORS preflight `/health` + `/api/v1/work-sessions` PATCH | 2/2 PASS |
| H5 | FE `/config.json` `api.baseUrl` = placeholder API host | PASS |

Observed H5:

```text
OK: http://app.doks.placeholder.metar-iwxxm.local/config.json
    api.baseUrl=http://api.doks.placeholder.metar-iwxxm.local
```

## Harness

- `scripts/deploy/doks_provisional_live_env.sh` — provisional `LIVE_*` / `VITE_API_BASE_URL`
- `scripts/deploy/verify_connectivity.sh` — Host-aware wake + H5 fetch when
  `PLAYWRIGHT_DOKS_PROVISIONAL=1` / `DOKS_PROVISIONAL=1`
- `tests/live_env.py` — `live_api_host_headers` / `live_frontend_fetch_base` /
  `expected_api_base_url`
- `tests/smoke/test_staging_connectivity.py` — H4 OPTIONS with Ingress `Host`
- Makefile: `test-live-connectivity-doks-provisional`

No `/etc/hosts` required (same pattern as T7.1 Playwright).

## Spec coverage

- connectivity-gates **H4** — CORS from FE origin
  `http://app.doks.placeholder.metar-iwxxm.local`
- connectivity-gates **H5** — runtime `config.json` points at placeholder API host
- `D-S038-tp` — H4–H5 required this cycle (provisional HTTP path under waive)
- Full HTTPS + real DNS H4–H5 remains deferred with `D-S038-t63-waive` until DNS lands

## Residual

- Republish `backend:ev031-doks` + `frontend:ev031-doks` when GHCR `write:packages` available
- T7.3 TC-F30-006 / TC-EV031-* next; T6.5 still soak-calendar blocked
