# Secrets only — copy to repo-root `.env`. Non-secrets: `config/local.json` or `config/prod.json`.
# See docs/env-contract.md and docs/env-sync-runbook.md

SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
DATABASE_URL=
ADMIN_EMAIL=
ADMIN_PASSWORD=
