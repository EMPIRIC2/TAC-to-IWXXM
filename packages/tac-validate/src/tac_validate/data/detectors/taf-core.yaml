# TAF core detectors (ADR-046 / M4 #1230).
# Residual: full A5-1 checklist stays in hatch_taf (python) — same honesty class
# as METAR R3 weather grammar. Pack id is the YAML SoT entrypoint.
schema_version: 1
id: taf-core
stage: token
products:
  - TAF
rules:
  - id: taf_python
    kind: python
    python: python:tac_validate.theme_checks:hatch_taf
