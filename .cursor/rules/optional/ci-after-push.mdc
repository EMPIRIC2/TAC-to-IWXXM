---
description: After every git push, watch GitHub CI until green on that branch
alwaysApply: false
---

# CI after push (blocking)

**A push is not done until required workflows are green** on that branch. Do not end the
session, open a PR as ready, or mark work complete after `git push` without watching.

## Workflows (monorepo)

| Branch / event | Workflow | Must pass |
|----------------|----------|-----------|
| Any PR / push | `.github/workflows/ci-cd.yml` | Test matrix (+ alembic/native/`e2e-smoke` as required) |
| `stage` push | same | **Deploy (stage)** + **Staging smoke** (after merge/push) |
| `main` push | same | Full CI; **no** auto prod Deploy (EV-051) |
| Tag `v*-*-deploy` or prod `workflow_dispatch` | same | Full CI + **Deploy (production)** |
| PR → `main` | same | **Staging gate** — head must be `stage` + tip **Staging smoke** green |

## Promote to main (DOKS / ADR-034)

Never open feature→`main`. Path: feature → PR → `stage` → Staging smoke green → PR
**`stage`→`main`** → Staging gate → merge → prod Deploy. Staging LB Host-header /
HTTPS probes use staging cluster IP (`STAGING_LB_IP`, default `143.244.202.13`), not
prod `168.144.12.70`. See `docs/deploy.md` §Promote and `doks-promote-from-stage.mdc`.

Workflows live at **monorepo root** (`../` from this backend package when checked out alone).

## After every push (required)

```bash
# From monorepo root
gh run list --branch "$(git branch --show-current)" --workflow ci-cd.yml --limit 3
gh run watch <run-id>
```

| Outcome | Action |
|---------|--------|
| **success** | Record run URL; proceed |
| **failure** | `gh run view <run-id> --log-failed` → fix → push → re-watch |
| **User waives** | AskQuestion only; note waiver in PR/summary |

## Before the first push (local parity — monorepo root)

```bash
make format-check    # ruff format --check apps packages tests + pnpm format:check
make lint
make typecheck
make test-unit       # or make ci for full pre-commit parity
```

Quality Gates scope is **`apps packages tests`** — not legacy `src/` paths. Bug repro tests
under `tests/bugs/` are both format/lint-checked **and executed**: they run as the `bugs`
matrix entry of the `ci-cd.yml` `test` job and via `make test-bugs` (included in
`make test-unit` / `make ci`). See `ci-quality-gates.mdc`.

## Related

- `atomic-commits.mdc` — local checks before commit
- `14-hotfix` / `09-qa` skills — full parity lists
- `docs/deploy.md` §Runbook
