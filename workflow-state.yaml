# Brownfield stub — EV-docs-numpy-corpus
#
# Live pipeline state for pack orchestrators lives under:
#   ~/.cursor/workflow/EMPIRIC2/TAC-to-IWXXM/sessions/{id}/
#     state.yaml, routing-plan.md, HANDOFF.md, reports/, evidence/
#
# The historical multi-MB workflow-state.yaml blob is preserved on the orphan
# git branch `docs-archive` (path: workflow-state.yaml).
#
# Do not reintroduce a large in-repo workflow-state as the session database.

schema_version: 1
status: relocated
session_store: ~/.cursor/workflow/EMPIRIC2/TAC-to-IWXXM/sessions/
archive_branch: docs-archive
note: >
  This stub replaces the legacy root workflow-state.yaml. Agents must use
  session-store paths for new cycles. See docs/CORPUS.md and
  docs/decisions/ev-docs-numpy-corpus.md.
