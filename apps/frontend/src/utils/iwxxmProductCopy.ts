/**
 * Operator-visible copy for IWXXM product pass-through (F7.t).
 *
 * Keep free of planning ids. [Corpus: product §F7]
 */

/** Help under the product select when IWXXM is chosen. */
export const IWXXM_PRODUCT_HELP =
  'Product IWXXM is pass-through: paste or upload XML to lint and validate. TAC text is rejected. Convert does not run TAC→IWXXM. Dedicated Validate mode remains available.';

/** Primary convert-button label when product is IWXXM. */
export const IWXXM_PRODUCT_CONVERT_LABEL = 'Lint & validate';

/** Accessible name for the convert button when product is IWXXM. */
export const IWXXM_PRODUCT_CONVERT_ARIA =
  'Lint and validate IWXXM XML (no TAC convert)';
