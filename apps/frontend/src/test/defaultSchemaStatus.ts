/** Default schema-status payload for FileConverter mount tests (EV-073). */
export const DEFAULT_SCHEMA_STATUS = {
  profile_pins: {
    ca_eccc: { extension_bundle_available: true, iwxxm_version: '3.0.0' },
  },
} as const;
